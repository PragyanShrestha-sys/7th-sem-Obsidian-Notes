Semi-structured data lies between structured and unstructured, using tags, markers, or hierarchies to separate semantic elements, but without the rigidity of a tabular format.

**Examples:** JSON, XML, YAML files, HTML code, NoSQL databases.

**Key Traits:** More flexible than structured, easier to organize than unstructured, machine-readable.
